class Main
{

}

class Test extends Main
{
	Boolean state = true
	Int value
	Boolean make_class()
	{
		if ((value > 3) || (value <= -5)) (
			state
		)
		else (
			false
		)
	}

  	Int get_value(Int temp_value, Boolean exact)
	{
		if (!exact) (
			value = temp_value
		)
		else (
			193094 - -8
		)
	}

	String local_string = "foobar"

	Int count()
	{
		value
	}
}