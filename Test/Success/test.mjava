class Main
{
	String name
	Int set_value(Int temp_value, Boolean exact){
		1
	}
}

class Test extends Main
{
	Boolean state = true
	Int value
	Boolean bar()
	{
		Boolean foo = false in {
			if ((value > 3) || (value <= -5)) (
				"foo" == "foo"
			)
			else (
				foo
			);
			foo = true
		}
	}

  	Int set_value(Int temp_value, Boolean exact)
	{
		if (!exact) (
			value = temp_value
		)
		else (
			193094 - -8
		)
	}

	String local_string = "foobar"
	Boolean fuz = state

	Int count()
	{
		this.set_value(4,state)
	}
}

Test test = new Test in {
	Int a = test.set_value(4,false) in{
		a+test.count()
	}
}
