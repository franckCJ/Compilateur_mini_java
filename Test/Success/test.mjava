class Main
{
	Int value
	String name
	Int set_value(Int temp_value, Boolean exact){
		value = temp_value
	}
}

class Test extends Main
{
	Boolean state = true
	Boolean bar()
	{
		Boolean foo = false in {
			if ((value > 3) || (state && fuz)) (
				"foo" == "foo"
			)
			else (
				foo
			);
			foo = true
		}
	}

  	Int set_value(Int temp_value, Boolean exact)
	{
		if (!exact) (
			value = temp_value
		)
		else (
			(193094 - -8) % 51
		)
	}

	String local_string = "foobar"
	Boolean fuz = state

	Int count()
	{
		this.set_value(4,state)
	}
}

/* multiline comment
 * this comment should be
 * ignored
 */

Test test = new Test in 
{

	//This is a one line comment
	Int a = test.set_value(4,test.bar() instanceof String) in
	{
		a+test.count()
	}
}
