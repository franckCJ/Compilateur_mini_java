class Main
{

}

toto = 0+0

class Test extends Main
{
	static Boolean global_state = true;
	static Integer global_value;
	static Boolean make_class()
	{
		if ((global_value > 3) || (global_value <= -5)) {
			global_state
		}
		else {
			false
		}
	}

  Integer get_value(Integer temp_value, Boolean exact)
	{
		if (!exact) {
			value = global_value;
			value
		}
		else {
			193094 - -8
		}
	}

	String local_string = null;

	Integer count()
	{
		local_string.length(3)
	}
}